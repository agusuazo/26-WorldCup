# Copiar a .env y completar (gitignored).

# Connection string de Supabase (Session pooler, puerto 5432).
# Con esto, tu PC comparte bet log y resultados manuales con la web.
SUPABASE_DB_URL=

# The Odds API (opcional, cuotas en vivo)
ODDS_API_KEY=
